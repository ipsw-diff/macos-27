name: Discover macOS 27 firmware

on:
  pull_request:
    paths:
      - .github/workflows/discover.yml
      - manifests/**
      - track.json
  workflow_dispatch:

permissions: {}

concurrency:
  group: macos-27-discovery-${{ github.ref }}
  cancel-in-progress: false

jobs:
  discover:
    permissions:
      contents: read
    uses: ipsw-diff/catalog/.github/workflows/discover.yml@0fcdb7ea63f0cab3d2ccfc117ee530fcc697b2b6
    with:
      policy-path: track.json
      manifests-dir: manifests
